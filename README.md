# Docker Container Manager API

Прототип REST API для программного управления Docker-контейнерами.  
Написан на Go, использует официальный Docker SDK, не требует БД, брокеров или UI.

---

## ⚙️ Запуск

### Требования
- Установленный [Docker Engine](https://docs.docker.com/engine/install/)
- Пользователь добавлен в группу `docker` (для доступа к `/var/run/docker.sock`)
  ```bash
  sudo usermod -aG docker $USER && newgrp docker
  ```

### 1. Установка зависимостей
```bash
go mod tidy
```

### 2. Настройка (опционально)
Редактируйте `config/server.yaml`:
```yaml
HOST: localhost
PORT: 8080
```
Или переопределите через env:
```bash
PORT=9000 go run cmd/server/main.go
```

### 3. Запуск
```bash
# Режим разработки
go run cmd/server/main.go

# Сборка и запуск
go build -o docker-manager cmd/server/main.go
./docker-manager
```

Сервер запустится на `http://localhost:8080`.

---

## 📡 API

| Метод | Эндпоинт                     | Описание                          |
|-------|------------------------------|-----------------------------------|
| GET   | `/tasks`                     | Список всех контейнеров           |
| GET   | `/tasks/{uuid}`              | Информация о контейнере           |
| POST  | `/start`                     | Создать и запустить контейнер     |
| DELETE| `/delete/{id}`               | Удалить контейнер                 |
|-------|------------------------------|-----------------------------------|

---

## 📝 Примечания

- Все операции выполняются напрямую через Docker Engine API (Unix-сокет `/var/run/docker.sock`).  
- Сервер **не запускается в Docker-контейнере** — работает локально на хосте.  
- Безопасность: API предназначен **только для локального использования** (без аутентификации).
- Является только прототипом, основная работа будет закончена позже
